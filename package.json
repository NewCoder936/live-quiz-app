{
  "name": "live-quiz-app",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted Kahoot-style live quiz app for internal team training",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm:dev:server\" \"npm:dev:client\"",
    "build": "npm run build --prefix client",
    "start": "NODE_ENV=production node server/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
